# NPM 配置文件

# 使用 npm 官方源（发布前请确保配置正确）
registry=https://registry.npmjs.org/

# 如果使用私有仓库，请取消下面的注释并修改为您的私有仓库地址
# registry=http://your-private-registry.com/

# 自动保存依赖到 package.json
save=true
save-exact=false

# 使用精确的版本号
# save-exact=true

# 不创建 package-lock.json（可选）
# package-lock=false
